# 🏗️ LangChain AI Platform - 전체 아키텍처

## 📊 시스템 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                    외부 클라이언트                               │
│  (웹브라우저, Python SDK, cURL, Postman)                        │
└─────────────────────┬───────────────────────────────────────────┘
                      │ HTTP/HTTPS
                      ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 5: Integration & Automation (후순위)                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ n8n (포트 5678)                                          │   │
│  │ - 외부 시스템 연동 (Slack, Gmail, Notion)               │   │
│  │ - 스케줄링 및 이벤트 트리거                             │   │
│  │ - LangChain API 소비자                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────┬───────────────────────────────────────────┘
                      │ HTTP → LangChain API
                      ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 4: API Gateway                                           │
│  ┌───────────────────────────┐  ┌────────────────────────────┐ │
│  │ Nginx (포트 80/443)       │  │ FastAPI (포트 8000)        │ │
│  │ - 리버스 프록시            │  │ - REST API 엔드포인트      │ │
│  │ - SSL/TLS                 │  │ - Swagger 문서             │ │
│  │ - 라우팅                   │  │ - WebSocket (향후)        │ │
│  └───────────────────────────┘  └────────────────────────────┘ │
└─────────────────────┬───────────────────────────────────────────┘
                      │ API 호출
                      ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 3: AI Orchestration (핵심 🌟)                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ LangChain (src/chains/)                                  │  │
│  │ ├─ conversation.py  → 대화 체인 (메모리 기반)           │  │
│  │ ├─ rag_chain.py     → RAG (검색-생성)                   │  │
│  │ └─ [확장 가능]                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ LangGraph (src/agents/)                                  │  │
│  │ ├─ research_agent.py → 연구 에이전트 (멀티 스텝)        │  │
│  │ └─ [확장 가능]     → 다른 에이전트 추가 가능            │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Models (src/models/)                                     │  │
│  │ ├─ llm.py          → Ollama LLM 래퍼                    │  │
│  │ └─ embeddings.py   → 임베딩 모델                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────┬──────────────────────────┬───────────────────────┘
              │                          │
              ↓                          ↓
┌─────────────────────────┐   ┌──────────────────────────────────┐
│  Structured Data Layer  │   │  Vector/Embedding Data Layer     │
│  (Layer 2A)             │   │  (Layer 2B)                      │
│ ┌─────────────────────┐ │   │ ┌──────────────────────────────┐ │
│ │ PostgreSQL          │ │   │ │ Milvus (포트 19530)          │ │
│ │ (포트 5432)         │ │   │ │ ┌──────────────────────────┐ │ │
│ │                     │ │   │ │ │ etcd (메타데이터)        │ │ │
│ │ • users             │ │   │ │ └──────────────────────────┘ │ │
│ │ • conversation_*    │ │   │ │ ┌──────────────────────────┐ │ │
│ │ • documents         │ │   │ │ │ MinIO (객체 스토리지)    │ │ │
│ │ • document_chunks   │ │   │ │ └──────────────────────────┘ │ │
│ │ • agent_executions  │ │   │ │                              │ │
│ │ • rag_queries       │ │   │ │ • 문서 임베딩 (384차원)      │ │
│ └─────────────────────┘ │   │ │ • HNSW 인덱스 (빠른 검색)   │ │
│                         │   │ │ • 코사인 유사도 검색         │ │
│ 역할:                   │   │ └──────────────────────────────┘ │
│ - ACID 트랜잭션         │   │                                  │
│ - 관계형 데이터         │   │ 역할:                            │
│ - 메타데이터 관리       │   │ - 대규모 벡터 검색 (백만+)       │
│ - JOIN, 집계            │   │ - 시맨틱 유사도 검색             │
└─────────────────────────┘   │ - RAG 문서 검색                 │
                              └──────────────────────────────────┘
┌─────────────────────────┐   ┌──────────────────────────────────┐
│  Redis (포트 6379)      │   │  Attu (포트 8080)                │
│  - 대화 메모리 캐싱     │   │  - Milvus 관리 UI                │
│  - 세션 관리            │   │  - 컬렉션 브라우저                │
└─────────────────────────┘   └──────────────────────────────────┘
              ↓                          ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1: Infrastructure                                        │
│  ┌────────────────────┐  ┌──────────────────────────────────┐  │
│  │ Ollama             │  │ Monitoring Stack                 │  │
│  │ (포트 11434)       │  │ ┌──────────────────────────────┐ │  │
│  │                    │  │ │ Prometheus (포트 9090)       │ │  │
│  │ • llama2          │  │ │ - 메트릭 수집                │ │  │
│  │ • mistral         │  │ └──────────────────────────────┘ │  │
│  │ • codellama       │  │ ┌──────────────────────────────┐ │  │
│  │ • [커스텀 모델]   │  │ │ Loki (포트 3100)             │ │  │
│  └────────────────────┘  │ │ - 로그 저장소                │ │  │
│                          │ └──────────────────────────────┘ │  │
│  역할:                   │ ┌──────────────────────────────┐ │  │
│  - LLM 추론             │  │ │ Promtail                     │ │  │
│  - 텍스트 생성          │  │ │ - 로그 수집                  │ │  │
│  - 로컬 실행 (GPU)      │  │ └──────────────────────────────┘ │  │
│                          │ ┌──────────────────────────────┐ │  │
│                          │ │ Grafana (포트 3000)          │ │  │
│                          │ │ - 대시보드 시각화            │ │  │
│                          │ └──────────────────────────────┘ │  │
│                          └──────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

       Docker Network: ai-network (bridge)
```

---

## 🔄 데이터 플로우 예시

### 1. RAG 쿼리 플로우

```
[사용자]
  ↓ "LangChain이란?"
[FastAPI: POST /api/rag/query]
  ↓
[RAGChain (src/chains/rag_chain.py)]
  ↓
[1] 쿼리 임베딩 생성
  ↓ embedding_model.embed_text()
[HuggingFace Embeddings] → [384차원 벡터]
  ↓
[2] Milvus 벡터 검색
  ↓ milvus_retriever.search()
[Milvus] → 상위 5개 유사 문서 반환
  ↓
[3] PostgreSQL에서 메타데이터 조회 (선택)
  ↓
[4] 컨텍스트 구성
  ↓ "문서1: ...\n문서2: ..."
[5] LLM으로 답변 생성
  ↓ llm.predict(prompt + context)
[Ollama: llama2] → "LangChain은..."
  ↓
[FastAPI] → JSON 응답
  ↓
[사용자] ← {answer, sources, scores}
```

### 2. LangGraph 에이전트 플로우

```
[사용자]
  ↓ "AI 보안 트렌드 조사"
[FastAPI: POST /api/agents/execute]
  ↓
[ResearchAgent (src/agents/research_agent.py)]
  ↓
[StateGraph 시작]
  ↓
[Step 1: research_node]
  ├─ Milvus에서 관련 문서 검색
  └─ state["research_results"] 업데이트
  ↓
[Step 2: analyze_node]
  ├─ LLM으로 정보 분석
  └─ state["analysis"] 업데이트
  ↓
[Step 3: write_report_node]
  ├─ LLM으로 리포트 작성
  └─ state["final_report"] 업데이트
  ↓
[Step 4: review_node]
  ├─ 품질 체크
  └─ state["status"] = "approved" or "needs_rewrite"
  ↓
[조건부 엣지]
  ├─ "approved" → END
  └─ "needs_rewrite" → write_report_node (재작성)
  ↓
[FastAPI] → JSON 응답
  ↓
[사용자] ← {result, steps, execution_time}
```

### 3. 문서 업로드 플로우

```
[사용자]
  ↓ PDF 파일 업로드
[FastAPI: POST /api/documents/upload]
  ↓
[DocumentProcessor (src/retrievers/document_processor.py)]
  ↓
[1] 임시 파일 저장
  ↓
[2] 문서 로딩
  ↓ PyPDFLoader
[PDF → LangChain Documents]
  ↓
[3] 청킹
  ↓ RecursiveCharacterTextSplitter(chunk_size=1000)
[42개 청크 생성]
  ↓
[4] 임베딩 생성
  ↓ embedding_model.embed_texts()
[42개 벡터 (각 384차원)]
  ↓
[5] Milvus에 삽입
  ↓ milvus_retriever.insert()
[Milvus 컬렉션에 저장]
  ↓
[6] PostgreSQL에 메타데이터 저장
  ↓ INSERT INTO documents, document_chunks
[PostgreSQL: 문서 정보 + Milvus ID 매핑]
  ↓
[FastAPI] → JSON 응답
  ↓
[사용자] ← {document_id, chunk_count, status}
```

---

## 🎯 각 컴포넌트의 역할

### **Layer 3: AI Orchestration (가장 중요)**

#### LangChain
- **위치**: `langchain_app/src/chains/`
- **역할**: 재사용 가능한 AI 파이프라인
- **예시**:
  - `conversation.py`: Redis 메모리 기반 대화
  - `rag_chain.py`: 검색-생성 체인

#### LangGraph
- **위치**: `langchain_app/src/agents/`
- **역할**: 복잡한 멀티 스텝 워크플로우
- **예시**:
  - `research_agent.py`: 정보 수집 → 분석 → 작성 → 검토

#### Retrievers
- **위치**: `langchain_app/src/retrievers/`
- **역할**: 데이터 검색 및 처리
- **예시**:
  - `milvus_retriever.py`: 벡터 검색
  - `document_processor.py`: 문서 업로드 처리

### **Layer 2: Data Layer**

#### PostgreSQL (구조화된 데이터)
- 사용자 정보
- 대화 세션 및 메시지
- 문서 메타데이터
- 에이전트 실행 로그
- RAG 쿼리 로그

#### Milvus (벡터 데이터)
- 문서 임베딩
- 시맨틱 검색
- 유사도 기반 검색
- 대규모 벡터 처리 (백만+ 벡터)

#### Redis (캐싱)
- 대화 메모리
- 세션 데이터
- LLM 응답 캐싱

### **Layer 1: Infrastructure**

#### Ollama
- 로컬 LLM 실행
- GPU 가속 (선택)
- 다양한 모델 지원

#### Monitoring
- **Prometheus**: 메트릭 수집
- **Loki**: 로그 저장
- **Promtail**: 로그 수집
- **Grafana**: 시각화

---

## 📦 배포 구성

### Docker Compose 파일

1. **docker-compose.yml** (기존)
   - Ollama
   - Nginx
   - n8n (후순위)
   - Monitoring (Prometheus, Grafana, Loki)

2. **docker-compose.langchain.yml** (새로 추가)
   - PostgreSQL
   - Milvus (+ etcd, MinIO)
   - Redis
   - LangChain API
   - Attu (Milvus UI)

### 시작 순서

```bash
# 1단계: 기존 인프라
docker compose up -d ollama prometheus grafana

# 2단계: LangChain 스택
docker compose -f docker-compose.langchain.yml up -d

# 또는 빠른 시작:
./start-langchain.sh
```

---

## 🔌 통합 포인트

### n8n → LangChain API 통합

```
[n8n 워크플로우]
  ↓
[HTTP Request 노드]
  - URL: http://langchain-api:8000/api/rag/query
  - Method: POST
  - Body: {"question": "..."}
  ↓
[LangChain API 처리]
  ↓
[응답 받음]
  ↓
[Slack 노드] → 알림 전송
```

### 외부 클라이언트 → API

- **REST API**: `http://localhost:8000/api/...`
- **Swagger UI**: `http://localhost:8000/docs`
- **Python SDK**: `httpx` 사용
- **cURL**: 직접 호출

---

## 📊 확장 포인트

### 새 체인 추가
```python
# src/chains/summarization_chain.py
class SummarizationChain:
    # 구현
```

### 새 에이전트 추가
```python
# src/agents/code_agent.py
class CodeAgent:
    # LangGraph 구현
```

### 새 API 엔드포인트
```python
# api/routes/summarization.py
@router.post("/summarize")
async def summarize():
    # 구현
```

---

## 🎓 학습 경로

1. **기초**: `LANGCHAIN_GUIDE.md` 읽기
2. **실습**: `API_EXAMPLES.md`로 API 테스트
3. **개발**: `langchain_app/README.md`로 코드 이해
4. **확장**: 새 체인/에이전트 추가

---

**이 아키텍처는 확장 가능하고, 테스트 가능하며, 프로덕션 레디입니다! 🚀**
